[main]
# Listen on port 25 (SMTP) and 587 (submission)
listen=[::0]:25,[::0]:587

# Connection limits
max_connections=1000
max_unrecognized_commands=10
max_errors=10

# Timeouts (in seconds)
connect_timeout=60
data_timeout=600

nodes=0
