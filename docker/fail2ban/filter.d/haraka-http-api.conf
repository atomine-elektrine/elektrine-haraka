# Fail2ban filter for Haraka HTTP API abuse

[Definition]

# Match API abuse patterns
failregex = ^<HOST> - .* "POST /api/.*" 401 .*$
            ^<HOST> - .* "POST /api/.*" 429 .*$
            ^<HOST> - .* "POST /api/.*" 400 .*$
            ^<HOST> - .* ".* /api/.*" 404 .*$

ignoreregex =